import 'package:flutter/material.dart';

const primaryColor = Color(0xFF6456B3);

const materialPrimaryColor = MaterialColor(
  0xFF0E7AC7,
  <int, Color>{
    50: primaryColor,
    100: primaryColor,
    200: primaryColor,
    300: primaryColor,
    400: primaryColor,
    500: primaryColor,
    600: primaryColor,
    700: primaryColor,
    800: primaryColor,
    900: primaryColor,
  },
);

const title = "URL Shortener";
